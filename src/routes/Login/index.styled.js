import styled from 'styled-components'
import { BasePageLayout, ModalContainer } from '../Root/index.styled'

export const LoginContainer = styled(BasePageLayout)``
export const LoginWrapper = styled(ModalContainer)``